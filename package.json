{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.17.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --force-local --hoist",
    "postinstall": "npm run bootstrap"
  }
}
